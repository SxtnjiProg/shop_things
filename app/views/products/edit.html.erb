<div class="container mt-5">
  <h1 class="mb-4">Редагування товару: <%= @product.name %></h1>
  
  
  <%= render 'form', product: @product %>
  
  <%= link_to '← Назад до товару', @product, class: 'btn btn-secondary mt-3' %>
</div>