<!DOCTYPE html>
<html lang="">
  <head>
    <title>ShopThings</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
    
    <%# ПІДКЛЮЧЕННЯ: Bootstrap Icons для піктограм %>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.min.css">
    
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js" integrity="sha384-YvpcrYf0tY3lHB60NNkmXc5s9fDVZLESaAA55NDzOxhy9GkcIdslK1eN7N6jIeHz" crossorigin="anonymous"></script>
    <%= javascript_importmap_tags %>
    <%= yield :head %>
  </head>
  
  <nav class="navbar navbar-expand-lg navbar-light bg-light">
    <div class="container position-relative">
      
      <%# ЛІВА СЕКЦІЯ (Home, Categories) %>
      <div class="navbar-nav me-auto mb-2 mb-lg-0 d-flex flex-row" style="gap: 1.5rem;">
        <%= link_to 'Home', root_path, class: 'nav-link' %>
        <%# Приклад посилання з випадаючим меню, якщо потрібно %>
        <%# Випадаюче меню категорій %>
        <div class="nav-item dropdown">
          <a class="nav-link dropdown-toggle" href="#" id="navbarDropdownCategories" role="button" data-bs-toggle="dropdown" aria-expanded="false">
            Categories
          </a>
          <ul class="dropdown-menu" aria-labelledby="navbarDropdownCategories">
            <%# Перебираємо всі категорії %>
            <% Category.all.each do |category| %>
              <li>
                <%= link_to category.name, category, class: "dropdown-item" %>
              </li>
            <% end %>
            
            <li><hr class="dropdown-divider"></li>
            
            <%# Посилання на сторінку керування (для всіх) %>
            <li>
              <%= link_to "Всі категорії (Керування)", categories_path, class: "dropdown-item fw-bold" %>
            </li>
          </ul>
        </div>
      </div>
      
      <%# ЦЕНТРАЛЬНИЙ ЛОГОТИП %>
      <a class="navbar-brand" href="/">Trippie Gluck</a>
      
      <%# ПРАВА СЕКЦІЯ (User/Cart) %>
      <div class="navbar-nav ms-auto d-flex flex-row align-items-center" style="gap: 1.5rem;">
        <% if user_signed_in? %>
          
          <%# --- ПОЧАТОК ЗМІН: Випадаюче меню профілю --- %>
          <div class="nav-item dropdown">
            <a class="nav-link dropdown-toggle" href="#" role="button" data-bs-toggle="dropdown" aria-expanded="false">
              <%= current_user.email.to_s.split('@').first %>
            </a>
            <ul class="dropdown-menu dropdown-menu-end">
              <li><%= link_to "Мій профіль", profile_path, class: "dropdown-item" %></li>
              <li><hr class="dropdown-divider"></li>
              <li><%= button_to "Вийти", destroy_user_session_path, method: :delete, class: "dropdown-item text-danger" %></li>
            </ul>
          </div>
          <%# --- КІНЕЦЬ ЗМІН --- %>

          <%# Іконка кошика (залишаємо як було) %>
          <%= link_to cart_index_path, class: 'nav-link' do %>
            <i class="bi bi-bag" style="font-size: 1.2rem;"></i> 
            (<%= session[:cart]&.values&.sum || 0 %>)
          <% end %>

        <% else %>
          
          <%# Single auth icon that links to the dedicated auth page %>
          <%= link_to auth_path, class: 'nav-link p-0', aria: { label: 'Sign in or register' } do %>
            <i class="bi bi-person-circle" style="font-size: 1.25rem;"></i>
          <% end %>
          
          <%# Іконка кошика для гостя %>
          <%= link_to cart_index_path, class: 'nav-link' do %>
             <i class="bi bi-bag" style="font-size: 1.2rem;"></i>
          <% end %>

        <% end %>
      </div>
    </div>
  </nav>

  <body>
    
    <%# ВІДОБРАЖЕННЯ Flash Messages %>
    <div class="container">
      <% if notice %>
        <div class="alert alert-success alert-dismissible fade show" role="alert">
          <%= notice %>
          <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
        </div>
      <% end %>
      <% if alert %>
        <div class="alert alert-danger alert-dismissible fade show" role="alert">
          <%= alert %>
          <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
        </div>
      <% end %>
      
      <%# Toast logic %>
      <% flash.each do |key, msg| %>
        <% next if msg.blank? %>
        <% toast_type = case key.to_s %>
          <% when 'notice' then 'success' %>
          <% when 'alert' then 'error' %>
          <% else 'info' %>
        <% end %>
        <div class="initial-toast" data-toast-type="<%= toast_type %>" data-toast-message="<%= j msg %>"></div>
      <% end %>
    </div>
    
    <%= yield %>

    <%# Toast container for in-page notifications (bottom-right) %>
    <div data-controller="toast" class="app-toast-container" aria-live="polite" aria-atomic="true"></div>

    <%# ФІНАЛЬНИЙ ФУТЕР - СТИЛЬ TRIPPIE GLUCK %>
    <footer class="main-footer">
      <div class="container footer-links">
        <p class="footer-copyright">
          &copy; <%= Time.current.year %>, Trippie Gluck
        </p>
        
        <%= link_to 'Refund policy', '#', class: 'text-decoration-none' %>
        <span class="separator">·</span>
        <%= link_to 'Privacy policy', '#', class: 'text-decoration-none' %>
        <span class="separator">·</span>
        <%= link_to 'Terms of service', '#', class: 'text-decoration-none' %>
        <span class="separator">·</span>
        <%= link_to 'Shipping policy', '#', class: 'text-decoration-none' %>
        <span class="separator">·</span>
        <%= link_to 'Contact information', '#', class: 'text-decoration-none' %>
      </div>
    </footer>
    
  </body>
</html>