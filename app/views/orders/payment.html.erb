<div class="container py-5">
  <div class="row justify-content-center">
    <div class="col-md-6">
      <div class="card border-0 shadow-sm rounded-4">
        <div class="card-header bg-dark text-white p-4 rounded-top-4">
          <h4 class="mb-0"><i class="bi bi-credit-card me-2"></i>Оплата замовлення</h4>
        </div>
        
        <div class="card-body p-4">
          <div class="text-center mb-4">
            <p class="text-muted mb-1">Замовлення #<%= @order.id %></p>
            <h2 class="fw-bold">€<%= @order.total_price %></h2>
          </div>

          <div class="mb-3">
            <label class="form-label text-muted small">Номер картки</label>
            <div class="input-group">
              <span class="input-group-text bg-white border-end-0"><i class="bi bi-credit-card-2-front"></i></span>
              <input type="text" class="form-control border-start-0 ps-0" placeholder="0000 0000 0000 0000">
            </div>
          </div>

          <div class="row mb-4">
            <div class="col-6">
              <label class="form-label text-muted small">Термін дії</label>
              <input type="text" class="form-control" placeholder="MM/YY">
            </div>
            <div class="col-6">
              <label class="form-label text-muted small">CVV</label>
              <input type="text" class="form-control" placeholder="123">
            </div>
          </div>

          <div class="alert alert-info small">
            <i class="bi bi-info-circle me-1"></i> Це демонстраційна оплата. Гроші не списуються.
          </div>

          <%= button_to process_payment_order_path(@order), method: :patch, class: "btn btn-dark w-100 py-3 fw-bold rounded-3" do %>
            ОПЛАТИТИ €<%= @order.total_price %>
          <% end %>
          
          <div class="text-center mt-3">
            <%= link_to "Скасувати", root_path, class: "text-muted small text-decoration-none" %>
          </div>

        </div>
      </div>
    </div>
  </div>
</div>